var app=angular.module("app",[])

app.controller("ConterollerName",function($scope){
    //array of strings 
    $scope.nav=[
    {
        href:"index.html",
        tetx:"Home",
    },
] 

$scope.pageInfo = "" //read the json and convert it into list of objects
$scope.hero =$scope.pageInfo.hero
$scope.
});